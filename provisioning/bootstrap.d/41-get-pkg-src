if [ "${USE_SAVED_PKG_SRC}" == "True" ]; then 
   echo "BCE: unzipping source package files..."  
   cd /tmp && \
   mkdir /tmp/pkg-src && \
   mv pkgs.zip /tmp/pkg-src && \
   cd /tmp/pkg-src && \
   unzip pkgs.zip && \
   mv */* /tmp && \
   echo "  DONE"  || echo "  FAIL"
fi
